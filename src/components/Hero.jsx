"use client";

import Carousel from "./Carousel";

const Hero = () => {
  return (
    <section className="relative isolate text-white overflow-hidden">
      <Carousel />
    </section>
  );
};

export default Hero;
